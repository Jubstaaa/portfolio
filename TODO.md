# TODO

Items surfaced by `bun run content:migrate`. Replace / review / resolve before the site goes live.

## blog

- post "my-current-tech-stack" — pick tags (currently empty) and decide if featured
- post "anime-series-i-have-enjoyed-over-the-years" — pick tags (currently empty) and decide if featured
- post "games-i-have-been-enjoying-lately" — pick tags (currently empty) and decide if featured
- post "how-to-debug-hono-applications-with-hono-telescope" — pick tags (currently empty) and decide if featured

## experiences

- "Macellan SuperApp" — fill highlights[] and stack[] (both currently empty)
- "Bishy" — fill highlights[] and stack[] (both currently empty)
- "Freelance" — fill highlights[] and stack[] (both currently empty)
- "Engin Tasarım" — fill highlights[] and stack[] (both currently empty)
- "13Brave Media" — fill highlights[] and stack[] (both currently empty)
- "A101 Yeni Mağazacılık A.Ş." — fill highlights[] and stack[] (both currently empty)

## projects

- "pixel-guess-game" — fill highlights[], review role/status, verify year=2025
- "prosbase" — fill highlights[], review role/status, verify year=2025
- "portfolio-site" — fill highlights[], review role/status, verify year=2025
- "bishy" — fill highlights[], review role/status, verify year=2025
- "portfolio-site-old" — fill highlights[], review role/status, verify year=2025
- "league-of-legends-daily-lore-video-bot" category "Automation / Bot" → fell back to "other" — review
- "league-of-legends-daily-lore-video-bot" — fill highlights[], review role/status, verify year=2025
- "lol-config-editor" category "Desktop App" → fell back to "other" — review
- "lol-config-editor" — fill highlights[], review role/status, verify year=2025
- "emoji-guess-game" — fill highlights[], review role/status, verify year=2025
- "my-pixel-world" — fill highlights[], review role/status, verify year=2025
- "hono-telescope" category "Developer Tool" → fell back to "other" — review
- "hono-telescope" — fill highlights[], review role/status, verify year=2025

## site

- replace placeholder url "https://example.com" with real domain
- review description — currently "Senior frontend engineer building fast, accessible interfaces."

## skills

- review category for each skill (all currently "frontend")

## socials

- "Reddit" icon "mdi:reddit" → "circle-user" (Lucide lacks a native one) — review
- "Spotify" icon "mdi:spotify" → "music" (Lucide lacks a native one) — review
- "Discord" icon "ri:discord-fill" → "message-square" (Lucide lacks a native one) — review

## stacks

- "Sequelize" has no curated category → defaulted to "library" — pick correct one
- "HTML" has no curated category → defaulted to "library" — pick correct one
- "npm" has no curated category → defaulted to "library" — pick correct one
- "CSS" has no curated category → defaulted to "library" — pick correct one
- "NextJS" has no curated category → defaulted to "library" — pick correct one
- "Node.js" has no curated category → defaulted to "library" — pick correct one
- "Electron.js" has no curated category → defaulted to "library" — pick correct one
- "Unity" has no curated category → defaulted to "library" — pick correct one
- "jQuery" has no curated category → defaulted to "library" — pick correct one
- "Postman" has no curated category → defaulted to "library" — pick correct one
- "Github Actions" has no curated category → defaulted to "library" — pick correct one
- "DigitalOcean" has no curated category → defaulted to "library" — pick correct one
- "Nginx" has no curated category → defaulted to "library" — pick correct one
- "Payload" has no curated category → defaulted to "library" — pick correct one
- "Ubuntu" has no curated category → defaulted to "library" — pick correct one

## image downloads

Any failures from `bun run content:download-images` are appended below.
